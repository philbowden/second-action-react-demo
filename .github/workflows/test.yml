name: Test Project
on: push
jobs:
    tests:
        runs-on: ubuntu-latest
        steps:
            - name: Get code
              uses: actions/checkout@v3
            - name: Insall dependencies
              run: npm ci
            - name: Run tests
              run: npm test